version: '3'

services:
  network-autosave:
    image: WHaoKong/network-autosave:latest
    container_name: network-autosave
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./config:/app/config
      - ./log:/app/log
    environment:
      - TZ=Asia/Shanghai 